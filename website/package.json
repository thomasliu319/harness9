{
  "name": "harness9-website",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "test": "node scripts/sync-docs.test.mjs",
    "predev": "npm run sync",
    "dev": "vitepress dev",
    "prebuild": "npm run sync",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "1.6.3"
  }
}
